#include <bits/stdc++.h>

using namespace std;

int main() {
	cout << "Hello, World!\n";
	int n;
	cin >> n;
	fflush(stdin);
	fclose(stdin);
	cout << "Square of " << n << " is " << n * n << endl;
	fflush(stdout);
	fclose(stdout);
	return 0;
}